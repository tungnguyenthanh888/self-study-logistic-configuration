server:
  port: 8761

management:
  endpoint:
    health:
      show-details: always
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka/